# Real World株式会社 — CLAUDE.md

Claude Code をこのリポジトリで使う際の運用ルール集。

---

## § 1. プロジェクト概要

Real World株式会社の業務自動化プロジェクト。
現在は LINE Messaging API を用いた顧客対応ボットと、経理部門の AI 化を並行開発中。

- **売上目標**：2〜3年で 2〜3億円規模
- **事業区分**：車両販売 / リース / サブスク / 中古車販売

---

## § 2. 技術スタック

| レイヤー | 技術 |
|---------|-----|
| ランタイム | Node.js >= 18 |
| フレームワーク | Express |
| LINE SDK | @line/bot-sdk |
| 会計ソフト | freee |
| テスト | Jest |
| リンター | ESLint |

---

## § 3. コーディング規約

- コメントは「なぜ（Why）」だけ書く。何をしているかはコードを読めばわかる
- エラーハンドリングはシステム境界（webhook 受信・外部 API 呼び出し）にのみ追加する
- 新機能は既存ファイルの修正を優先し、不要な新ファイルは作らない

---

## § 4. Git 運用

- コミットメッセージは英語で簡潔に（例：`feat: add receipt OCR handler`）
- 機能ブランチは `feature/` プレフィックス、Claude 作業ブランチは `claude/` プレフィックス
- `main` への直接プッシュは禁止。必ず PR を通す

---

## § 5. 環境変数

`.env.example` を参照。本番値は絶対にコミットしない。

```
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=
FREEE_CLIENT_ID=
FREEE_CLIENT_SECRET=
FREEE_COMPANY_ID=
CLOUD_STORAGE_PATH=
PORT=3000
```

---

## § 6. テスト方針

- `npm test` でユニットテストを実行
- webhook ハンドラの主要パスは必ずテストカバレッジを確保する
- 外部 API（LINE / freee）は必ずモックする

---

## § 7. デプロイ

```bash
npm start          # 本番起動
npm run dev        # 開発用（ファイル監視付き）
```

LINE チャンネルの webhook URL をサーバーの `/webhook` エンドポイントに向ける。

---

## § 8. LINE ボット設計方針

- `src/handler.js` がイベント処理の中心。新しいコマンドはここに追加する
- ユーザーへの返答は日本語で、簡潔に
- 経理関連のコマンドはプレフィックス `/keiri` で統一する

---

## § 9. 経理部の運用ルール

### 9-1. データの入口（自社責任）

- 領収書は受領後 **24時間以内** にスマホ撮影 → クラウドストレージ
- 請求書は受領後 **24時間以内** に PDF 化 → クラウドストレージ
- ファイル命名規則：`YYYY-MM-DD_取引先_金額_部門.pdf`
- 部門タグ：`car-sales` / `lease` / `subsc` / `used-car` / `common`

### 9-2. データの出口（税理士連携）

- 月次締め：当月分は **翌月5日まで** にクラウドストレージへ
- 試算表受領：翌月15日目安
- 月次MTG：翌月20〜25日のあいだ、**17時以降オンライン**

### 9-3. 経理AIの完了基準

- [ ] 領収書/請求書がクラウドにある
- [ ] 部門タグが付いている
- [ ] freee に連携済み or 税理士に共有済み
- [ ] 月次試算表のレビューコメントが残っている

### 9-4. 禁止事項（経理特有）

- ❌ 領収書を財布・カバンに溜める（その日のうちに撮影）
- ❌ 部門タグなしで税理士に丸投げ
- ❌ 試算表を「見ただけ」で放置（必ず Claude にレビューさせる）

### 9-5. 部門コード一覧

| コード | 事業内容 |
|--------|---------|
| `car-sales` | 車両販売 |
| `lease` | リース |
| `subsc` | サブスクリプション |
| `used-car` | 中古車販売 |
| `common` | 共通費（部門按分が必要なもの） |

### 9-6. 月次運用カレンダー

| タイミング | 作業 | 担当 |
|-----------|-----|-----|
| 当月：随時 | 領収書/請求書クラウド化（24h以内ルール） | 奥様＋AI |
| 翌月 1〜5日 | `/ryoshusho-shori` で当月分まとめ仕訳下書き | AI |
| 翌月 5日 | 税理士へクラウド共有 | 松澤さん |
| 翌月 15日目安 | 試算表受領 | 新税理士 |
| 翌月 15〜19日 | `/zeirishi-mtg` で論点抽出・質問リスト作成 | AI |
| 翌月 20〜25日 | 月次MTG（17時以降オンライン） | 松澤さん＋新税理士 |
| 翌月末 | `/cashflow-review` で翌3ヶ月予測アップデート | AI |

---

## § 10. 経理スラッシュコマンド一覧

| コマンド | 用途 |
|---------|-----|
| `/ryoshusho-shori` | 領収書写真→OCR→仕訳下書き→クラウド保存 |
| `/seikyusho-hakko` | 案件データ→請求書PDF→freee登録→顧客送付 |
| `/zeirishi-mtg` | 当月数字＋論点＋質問リスト自動生成 |
| `/cashflow-review` | 損益vs現金、部門別、3ヶ月先予測 |
